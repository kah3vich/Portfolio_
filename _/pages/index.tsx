import { Layout } from '@/layout';
import { FC } from 'react';

const Home: FC = () => {
	return (
		<Layout title='Home'>
			<div className='containers'></div>
		</Layout>
	);
};

export default Home;
